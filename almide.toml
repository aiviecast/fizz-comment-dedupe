[package]
name = "fizz_comment_dedupe"
version = "0.1.0"
description = "Fizz comment filter — drop duplicate Comments by id (FIFO-capped seen set), NDJSON in/out"

[dependencies]
fizz_protocol = { git = "https://github.com/aiviecast/fizz-protocol", tag = "v0.2.0" }
